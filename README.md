Building Supervised Actors: Bank Account System
🎯 What We're Building
A bank account system where each account is an actor that:

Handles deposits, withdrawals, and transfers
Maintains transaction history
Retries failed operations automatically
Can be restarted if something goes wrong
Validates operations (e.g., insufficient funds)

